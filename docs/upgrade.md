# 升级

当前版本是 `0.1.0-local`，还没有远程发布流程。

升级前：

1. 运行 `verify.ps1` 确认当前 Hermes 是否已有适配优化能力。
2. 运行 `install.ps1` 创建源码备份并验证。
3. 后续抽出源码补丁后，再补幂等安装和回滚验证。

后续正式版本需要补：

- 媒体和文件源码补丁。
- 忙碌队列源码补丁。
- `/retry` 附件保护补丁。
- 飞书线程/历史/资源下载工具补丁。
